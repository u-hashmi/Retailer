import React from 'react';
import { Box } from '@mui/material';

const BarChart = () => {
  return (
    <Box sx={{ height: '300px' }}>
       
    </Box>
  );
};

export default BarChart;
