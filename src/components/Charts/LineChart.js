import React from 'react';
import { Box } from '@mui/material';

const LineChart = () => {
  return (
    <Box sx={{ height: '300px' }}>
       
    </Box>
  );
};

export default LineChart;
