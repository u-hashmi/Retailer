import React from 'react';
import { Box } from '@mui/material';

const PieChart = () => {
  return (
    <Box sx={{ height: '300px' }}>
       
    </Box>
  );
};

export default PieChart;
